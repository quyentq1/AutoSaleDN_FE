name: Deploy React Frontend (Vite)

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Debug secrets usage
        run: |
          echo "Username: ${{ secrets.VPS_USERNAME }}"
          echo "Host: ${{ secrets.VPS_HOST }}"
          echo "Deploy path: ${{ secrets.FRONTEND_DEPLOY_PATH }}"
